## Choose one of the schedulers
##### OBS: Except for the py file, use it outside the container
```TEXT
scheduler/
    jobs.py          # APScheduler
    ubuntu.sh        # crontab
    windows.ps1      # Task Scheduler
    k8s.yml          # CronJob
    ```
